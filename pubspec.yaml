name: enet
description: >-
  ENet Dart reliable, low-latency UDP networking 
  for Flutter and Dart applications.
repository: https://github.com/Nika0000/enet
version: 0.1.0

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: '>=3.9.0-21.0.dev <4.0.0'

dependencies:
  code_assets: ^0.19.4
  ffi: ^2.1.4
  hooks: ^0.19.0
  native_toolchain_c: ">=0.16.1 <0.18.0"
  
  # For debug
  # logging: ^1.3.0
 
dev_dependencies:
  test: ^1.25.8
  very_good_analysis: ^9.0.0

topics:
  - udp
  - enet
  - enet-dart